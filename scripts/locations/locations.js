export const Locations = (locationObj) => {
    return (

        `<div classes="locationContainer">
            <ul >
                <li>${locationObj.tips}</li>
            <ul>
        </div>`
    )
}